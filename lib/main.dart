import 'package:mindforge/bootstrap.dart';

/// The process entrypoint. Everything it would otherwise do lives in
/// [bootstrap], where the ordering is visible in one place.
Future<void> main() => bootstrap();
